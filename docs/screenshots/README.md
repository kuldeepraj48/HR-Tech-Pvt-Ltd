# Screenshots for GitLab Setup and Steps

Add screenshots here to document GitLab setup and run steps.

**Suggested filenames (optional):**

- `01-gitlab-project-clone-url.png` — GitLab project page showing clone URL
- `02-terminal-clone.png` — Terminal after `git clone`
- `03-composer-install.png` — After `composer install`
- `04-env-and-key.png` — After `cp .env.example .env` and `php artisan key:generate`
- `05-migrate-seed.png` — After `php artisan migrate` and `php artisan db:seed`
- `06-artisan-serve.png` — After `php artisan serve`
- `07-login-page.png` — Login page in browser
- `08-dashboard.png` — Dashboard after login

In **GITLAB_SETUP_AND_STEPS.md** you can reference them like:

```markdown
![Clone from GitLab](screenshots/01-gitlab-project-clone-url.png)
```

*(Use relative path from repo root, e.g. `docs/screenshots/01-...` if linking from root.)*
